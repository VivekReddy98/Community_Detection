{
   "create": {"Node": "CREATE (<uid>:<label> {name=<name>, cluster=<cluster>})",
             "Relation": "(<uid1>)-[:<relation>]->(<uid1>)",
             "Index": "CREATE INDEX ON :<label>(<index>)",},
     "PageRank": {
        "query" : "CALL algo.pageRank.stream('MATCH (n:{label}) RETURN id(n) as id','MATCH (p1:{label})-[:KNOWS]->(p2:{label}) RETURN id(p1) as source, id(p2) as target',{graph:'cypher', iterations:20}) Yield nodeId, score RETURN algo.asNode(nodeId).name AS node,score ORDER BY score DESC;",
        "vars" : "labels",
        "labels" : "{label}" 
    }
}